#!/usr/bin/env bash
# app@install — installe le projet de zéro
#
# Ébauche : la commande annonce seulement son nom et les variables qu’elle reçoit.
set -euo pipefail

echo "app@install  EXEC=${EXEC:-}"
